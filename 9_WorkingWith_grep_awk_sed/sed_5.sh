3. sed — Modify Text

sed edits text stream-wise (without changing the file unless told).

#replaces only first occurence
Syntax: sed 's/old/new/' file_name 

#replace all occurence
sed 's/OLD/NEW/g' file_name